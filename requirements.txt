# Runtime dependencies (pinned for reproducible builds).
# Deliberately minimal: no Redis, no database driver, no pandas/numpy/scipy.

# Discord gateway client (pulls in aiohttp).
discord.py==2.4.0

# Tiny health-check web server for Render's Web Service.
fastapi==0.115.6
uvicorn==0.32.1

# Async HTTP client used only for optional webhook delivery.
httpx==0.28.1

# Loads a local .env during development (no-op in production).
python-dotenv==1.0.1

# Popular libraries
numpy
pandas
matplotlib
scipy
requests